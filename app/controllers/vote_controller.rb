class VoteController < ApplicationController
  # renders the page where a voter will decide what vote to cast
  def new
  end

  # handles the submission of a casted voted by a voter
  def cast
  end
end
